extends page-layout
include mixins/analyzer-info

block content

    h1.page-header BH2000 - WhereLikeMethod
    .row
        .col-sm-12
            p.lead
                | Method <code>WhereLike</code> or <code>WhereNotLike</code> of <code>CMS.DataEngine.WhereConditionBase{TInfo}</code> is used

            +analyzer-info('BH2000', 'WhereLikeMethodAnalyzer')


    //.row
    //    .col-sm-12
    //        h2 Motivation
    //        p TBD

    .row
        .col-sm-12
            h2 Code Fixes
            p Multiple code fixes will be presented. Based on whether <code>WhereLike</code> or <code>WhereNotLike</code> method is diagnosed, one of:
            ul
                li
                    code WhereContains
                    | &nbsp;or&nbsp;
                    code WhereNotContains
                li
                    code WhereStartsWith
                    | &nbsp;or&nbsp;
                    code WhereNotStartsWith
                li
                    code WhereEndsWith
                    | &nbsp;or&nbsp;
                    code WhereNotEndsWith

            p Before:
            pre
                | using CMS.DataEngine;
                | ...
                | var whereCondition = whereCondition.WhereLike("columnName", "value");

            p After:
            pre
                | using CMS.DataEngine;
                | ...
                | var whereCondition = whereCondition.WhereContains("columnName", "value");
                | // or
                | var whereCondition = whereCondition.WhereStartsWith("columnName", "value");
                | // or
                | var whereCondition = whereCondition.WhereEndsWith("columnName", "value");
