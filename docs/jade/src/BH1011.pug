extends page-layout
include mixins/analyzer-info
include mixins/conditional-access-code-fix-remark

block content

    h1.page-header BH1011 - PageIsPostBack
    .row
        .col-sm-12
            p.lead
                | Access to <code>IsPostBack</code> property of <code>System.Web.UI.Page</code>

            +analyzer-info('BH1011', 'PageIsPostBackAnalyzer')


    //.row
    //    .col-sm-12
    //        h2 Motivation
    //        p TBD

    .row
        .col-sm-12
            h2 Code Fixes
            p A code fix will be presented. The access will be replaced with <code>RequestHelper.IsPostBack()</code> expression.

            p Before:
            pre
                | var isCallback = Request.IsPostBack;

            p After:
            pre
                | using CMS.Helpers;
                | ...
                | var isCallback = RequestHelper.IsPostBack();

            +conditional-access-code-fix-remark()

